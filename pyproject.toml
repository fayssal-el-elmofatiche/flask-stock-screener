[tool.poetry]
name = "flask-mega-tutorial-finance"
version = "0.1.0"
description = ""
authors = ["Fayssal"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
